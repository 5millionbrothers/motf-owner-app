# 숙소 웹사이트 자동 등록 설정

## 1. Supabase

SQL Editor에서 `motf-database/supabase/66_stay_website_import.sql`을 한 번 실행합니다.

이 마이그레이션은 다음만 추가합니다.

- 웹사이트 분석 초안과 실행 이력
- 같은 사이트 중복 분석을 피하기 위한 콘텐츠 해시
- 등록 숙소의 공식 출처 URL
- 생성된 숙소와 가져오기 작업의 연결

비밀번호는 DB에 저장하지 않습니다.

## 2. Vercel 환경변수

운영자 앱 프로젝트의 Production 환경에 아래 값을 추가하고 재배포합니다.

```text
OPENAI_API_KEY=OpenAI API 프로젝트 키
OPENAI_STAY_IMPORT_MODEL=gpt-5.6-luna
MOTF_IMPORT_ACCOUNT_DOMAIN=motf.co.kr
```

기존 `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_URL`,
`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`도 계속 필요합니다. 주소 좌표 자동 확인에는
기존 `NAVER_MAP_KEY_ID`, `NAVER_MAP_SECRET_KEY`를 사용합니다.

`OPENAI_API_KEY`는 ChatGPT 또는 Codex 구독과 별도인 API 사용량으로 청구됩니다.
브라우저에는 노출되지 않고 서버의 분석 API에서만 사용됩니다.

## 3. 사용법

1. 운영자 계정으로 사장님·본사 관리 페이지에 로그인합니다.
2. 왼쪽의 `숙소 자동 등록`을 엽니다.
3. 숙소가 제공한 공식 웹사이트 주소를 입력합니다.
4. 생성된 초안에서 기본 정보, 객실별 1박 가격, 인원, 추가요금과 사진을 확인합니다.
5. 사진·정보 사용 권한 확인 후 `검토 완료·숙소 등록`을 누릅니다.
6. 화면에 한 번 표시되는 임시 이메일과 비밀번호를 숙소 담당자에게 전달합니다.

숙소 담당자는 같은 사장님 페이지에서 로그인해 사진과 정보를 수정할 수 있습니다.
실제 사장님이 별도 계정을 만든 뒤에는 기존 `업장 소유권 이전` 기능으로 계정만
교체할 수 있고 숙소, 객실, 예약, 채팅 데이터는 그대로 유지됩니다.

## 운영 원칙

- 공식 숙소 사이트 또는 숙소가 제공한 자료만 가져옵니다.
- AI가 비운 값과 경고 항목은 사람이 확인합니다.
- 등록 버튼을 누르기 전에는 실제 숙소나 계정을 만들지 않습니다.
- 같은 콘텐츠는 저장된 초안을 재사용합니다. 다시 분석해야 하면 웹사이트 내용이
  바뀐 뒤 실행하거나 API에 `force: true`를 보내야 합니다.
- 자바스크립트로만 렌더링되고 서버 HTML에 정보가 없는 사이트는 일부 항목이
  비어 있을 수 있습니다. 이 경우 검토 화면에서 직접 보완합니다.
